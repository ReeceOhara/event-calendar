'use strict'

class DayEvent {
    constructor(title, time) {
        this.title = title;
        this.time = time;

        this.getTitle = function() {
            return this.getTitle;
        }
    }
}